# Tomcat
server:
  port: 9202

# Spring
spring:
  application:
    # 应用名称
    name: chiot-system
  profiles:
    # 环境配置
    active: dev

logging:
  file:
    path: ../logs/${spring.application.name}
  level:
    org.chdzq: debug
    root: info
  config: classpath:log/logback.xml