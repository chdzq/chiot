# Tomcat
server:
  port: 9201

# Spring
spring:
  application:
    # 应用名称
    name: chiot-authentication
  profiles:
    # 环境配置
    active: dev

logging:
  file:
    path: ../logs/${spring.application.name}
  level:
    root: info
  config: classpath:log/logback.xml