server:
  port: 9200

spring:
  # 应用名称
  application:
    name: chiot-gateway
  # 环境配置
  profiles:
    active: dev

logging:
  file:
    path: ../logs/${spring.application.name}
  level:
    root: info
  config: classpath:log/logback.xml
